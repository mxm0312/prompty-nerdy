# Benchmarks

There is no benchmark yet. Nothing here is a result, and no claim in this repo
is measured.

When it exists it will score answer quality, not prompt length: agreement with
gold labels, how often the output fails to match the declared contract, and how
often repeated runs on the same input disagree with each other.

---
description: Lint a prompt for determinism, readability and efficiency
argument-hint: "[path or paste the prompt]"
---

Use the prompty-lint skill to lint this prompt: $ARGUMENTS

If $ARGUMENTS is a path, read that file and lint its contents. If nothing was
given, ask for the prompt in one line and stop.

---
description: Generate breaking inputs for a prompt and show what it does with them
argument-hint: "[path or paste the prompt]"
---

Use the prompty-probe skill to find the inputs that break this prompt:
$ARGUMENTS

If $ARGUMENTS is a path, read that file and probe its contents. If nothing was
given, ask for the prompt in one line and stop.

---
description: Apply prompt lint fixes and return the full rewritten prompt
argument-hint: "[path or paste the prompt]"
---

Use the prompty-fix skill to fix this prompt and hand back the complete
rewritten version: $ARGUMENTS

If $ARGUMENTS is a path, read that file and fix its contents. If nothing was
given, ask for the prompt in one line and stop.

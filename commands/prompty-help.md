---
description: prompty-nerdy quick reference
---

Print the prompty-nerdy quick reference in one code block, no commentary: the three principles (determinism — for any input the instruction determines the answer; readability — a person can follow it and label data with it; efficiency — right techniques where needed, nothing that only confuses the model), the nine rules (no-undecided-input, no-contradiction, examples-match-rules, decision-terms-defined, disjoint-categories for classification only, output-contract, human-executable, reason-before-verdict, technique-fits-task), the two severities (error — an input exists for which the prompt does not determine the answer; warning — determined but a careful reader could land elsewhere), the requirement that every finding carries the input that proves it, the probe families (empty, fits nothing, fits several, boundary pairs, unexpected form, missing dependency, embedded instruction, adversarial but plausible), what is never a finding (length, reasoning or demonstrations as such, section order, retrieval quality, anything unprovable by an input), the commands (/prompty-lint, /prompty-fix, /prompty-probe, /prompty-help) and the rule that every fix is answer-neutral or named as a behavior change.
